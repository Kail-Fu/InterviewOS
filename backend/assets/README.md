This folder is served by the backend at `/assets` in local mode.

Bundled demo assessment:
- `example_assessment.zip`

Default local download URL:
- `http://localhost:8000/assets/example_assessment.zip`

To use your own file, either:
- replace `example_assessment.zip`, or
- change `LOCAL_ASSESSMENT_FILENAME` in `.env`.
