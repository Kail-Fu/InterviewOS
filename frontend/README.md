# InterviewOS Frontend

Minimal React/Vite app for starting an assessment invite.

## Local dev

```bash
cp .env.example .env
npm install
npm run dev
```

Default API target:

- `VITE_API_BASE_URL=http://localhost:8000`
